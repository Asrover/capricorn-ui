import { create } from '@storybook/theming/create';

export default create({
    base: 'light',
    brandTitle: 'Capricorn UI',
    // brandUrl: 'https://practika.net',
    // brandImage: `/${Logo}`,
    fontBase: '"Open Sans", sans-serif',
});
